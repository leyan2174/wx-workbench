# 项目角色、任务分工与启动约定

本文件定义可跨工具迁移的职责，不绑定某台电脑的任务 ID。当前由三个 Codex 任务承担；后续可映射为独立 Bot，职责、交付格式与权限边界保持不变。

## 角色清单

| 角色标识 | 当前任务名称 | 核心责任 | 写入归属 |
|---|---|---|---|
| architect-release | wx-workbench-架构与发布总负责 | 产品范围、架构边界、优先级、协调、独立验收与最终公开 | AGENTS.md、PROJECT_DELIVERY.md、PUBLICATION_BRIEF.md、本文件、仓库外审查材料 |
| implementation | wx-workbench-代码实现与发布准备 | 实现收口、源码首发候选、构建测试、许可资产处理、代码仓说明 | 生产代码、测试、构建/工作流、代码仓 README/NOTICE、代码旁接口说明 |
| documentation | wx-workbench-技术文档与知识发布 | 独立机制说明、证据分级、图与导航、文档候选 | 独立 wx-workbench-docs 目录及其文档、文档状态和规则 |

代码仓内已有 docs 的技术事实仍由编码任务同步，文档任务只读并整理到独立文档仓，不同时修改同一份文档。需要纠错时发送文件/符号、证据及建议，由归属任务修改。负责人可重新分配写入范围，但应先通知相关任务。

## 共同目标

尽快交付可公开、可复现的 wx-workbench 源码，以及不依赖原代码也能指导重新实现的 wx-workbench-docs。详见 PROJECT_DELIVERY.md 与 PUBLICATION_BRIEF.md。无必要重构不阻塞首发，安全/隐私/权利和准确性不因赶进度降低。

## 启动提示

### 架构与发布总负责

读取 AGENTS.md、PROJECT_DELIVERY.md、PUBLICATION_BRIEF.md、本文件和最新候选证据。确认目标、写入归属与阻塞，给实施和文档任务下达有边界的目标；复核候选而非仅转述报告，负责最终公开和结果验证。普通工程决策自主推进，真实外部阻塞向用户明确报告。

### 代码实现与发布准备

读取共同规则和负责人最新指令，冻结首发范围，实施必要修复并准备从干净环境可构建的候选。提供提交/树或文件清单标识、构建测试证据、功能限制、许可证和隐私检查结果。向文档任务提供当前实现和未完成事实，不要求文档任务猜测。不要自行改变远端可见性或触发 npm。

### 技术文档与知识发布

读取共同规则、当前源码/测试/文档索引及来源说明。只读代码仓，写独立文档仓；按微信机制、项目实现、通用设计分区，主内容足以脱离源码理解。标明证据、推测、版本、合成例子和边界，提交可审阅候选与覆盖缺口。不得把私人样本、内部绝对路径、未授权素材或未证实结论放入公开内容。

## 任务交付约定

负责人下达任务应包含：目标、范围、禁止触及项、输入证据、依赖、验收标准和产物位置。任务执行者回报应包含：当前状态、实际变更、候选版本/内容标识、验证证据、剩余缺口、下一步及是否需要协调。

状态使用：待执行、执行中、待验收、需修订、受阻、完成。消息发出不等于对方已接收；“测试通过”不等于“发布通过”；负责人验收后才关闭交付项。进行中的源码引用必须在发布冻结后重新对照。

源码/文档文件和 Git 版本是交付事实来源，任务聊天是协调记录，不能作为唯一项目状态库。正式决策和门槛更新到 PROJECT_DELIVERY.md；不要在共享文档写凭据、当前电脑任务 UUID 或私人运行路径。

## 未来迁移到职责 Bot

先迁移职责，不在首发前建设新的调度平台。三个角色可分别对应架构/发布 Bot、编码 Bot、文档 Bot。

迁移所需基础能力：共享且持久的任务状态、文件/Git 访问、角色写入权限、异步交付通知、候选版本标识、失败重试与重复执行保护、可追溯日志。架构/发布 Bot 统一操作发布；其他 Bot 不因收到重复消息而重复发包、覆盖产物或改可见性。

聊天入口、Bot 展示名和所用模型可以变化，角色职责和交付协议保持稳定。具体 Grok Bot 平台是否提供上述能力应在迁移阶段验证，不在此宣称平台已支持。不将现在的三段长对话原样复制视为迁移完成。
