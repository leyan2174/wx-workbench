# Windows 微信 4.1.10+ 密钥导出历史验证记录

## 当前状态与历史边界（2026-09-07 校订）

本文保留旧本地构建 `0.3.0-leyan.1` 对微信 `4.1.12.26` 的实机记录。下方版本、查询结果、消息数量及命令属于该历史样本；2026-09-07 是本次文档校订日期，不是重新实测日期。不能把旧成功推广到当前代码、其他机器或全部 `4.1.10+` 版本。

| 项目 | 当前源码或证据状态 | 限制 |
| --- | --- | --- |
| 只读内存扫描 | [windows.rs](../src/scanner/windows.rs) 仍按 `< 4.1.10` / `>= 4.1.10` 选择 raw-key / Config.Cipher；扫描使用受检查库存与页一校验 | 版本阈值是路由规则，不是所有后续构建的兼容保证；新版扫描失败不自动降级为旧布局 |
| 默认密钥来源 | [scanner/mod.rs](../src/scanner/mod.rs) 的 `auto` 先尝试 DPAPI 复用，失败再只读扫描；`memory` 明确跳过复用 | 当前 `wx init --force` 不一定扫描内存，历史命令不能直接当作现在的强制扫描语义 |
| 显式账号捕获 | `account` 要求 `--force --restart-wechat`，使用 Frida Hook 并重启微信 | 不属于本文“非注入、只读进程扫描”的实测路径；详见 [账号密钥提供器](account-key-provider.md) |
| 初始化保存 | [init.rs](../src/cli/init.rs) 尊重选中配置的 `keys_file`，默认 `all_keys.json`；保留未知字段并逐文件原子发布 | 密钥、配置及可选 DPAPI 记录不是跨文件事务；配置发布失败不报告初始化成功 |
| 当前主线自动化 | Rust 精简后 `1325/0/11 ignored`、个人 Web `61/0`；8 次可选执行另行通过，MSVC check 保留 9 警告 | 来源为主线 [迁移总账](rust-migration.md)，不代表本轮重跑实机取钥、真实账号/媒体、模型/GPU 或真实云端验证 |

最新整体状态与日志出处见 [系统架构](architecture.md)、[Rust 迁移记录](rust-migration.md)。企业微信排除本轮目标，保留其既有代码与入口；本文不提供企微兼容结论。

文档同步期间集中重跑验证：`C:/CodexLocal/wx-cli-doc-sync-tests.log` 退出 0，20 套件 `1325/0/11`；check 退出 0、9 类警告，18 项 EXE help 检查通过。本次没有重跑实机取钥，help 成功不代表真实微信或部署验收。

若今后要复核当前只读扫描，应先显式设置 `WX_CLI_CONFIG` 选择账号配置，再使用 `wx init --force --db-dir <目标db_storage> --key-provider memory`。这是需另行授权的实机验证步骤，本轮未执行。

## 历史结论

原记录报告：在当时 Windows 本机和旧本地构建上，对微信 `4.1.12.26` 完成端到端验证，能够从正在运行且已登录的微信进程中读取数据库密钥，并解密 `db_storage` 下的消息数据库。

这次验证覆盖了完整链路：

1. 读取 `Weixin.exe` 文件版本。
2. 根据版本选择对应的密钥提供器。
3. 只读扫描微信进程内存中的 `Config.Cipher` 数据。
4. 使用数据库第一页的 SQLCipher HMAC 校验候选密钥。
5. 将已验证的数据库密钥写入本机 `all_keys.json`。
6. 跨消息数据库分片读取会话、历史消息和统计信息。

## 历史实测环境

| 项目 | 实测值 |
| --- | --- |
| 平台 | Windows x64 |
| 微信进程 | `Weixin.exe` |
| 微信版本 | `4.1.12.26` |
| wx-cli | `0.3.0-leyan.1` 本地 release 构建 |
| 测试会话 | 本机已有群聊数据 |

不将密钥、原始数据库、账号目录或聊天内容提交到仓库。文中的会话名称仅用于说明验证命令，测试数据仍只保留在本机。

## 历史验证命令

微信保持运行并完成登录后，在 Windows PowerShell 中执行：

```powershell
wx init --force
wx sessions --json
wx history "七龙珠" --limit 100 --json
wx stats "七龙珠" --json
```

当时使用 `wx init --force` 触发扫描和密钥验证；现在默认 `auto` 可先复用 DPAPI，当前强制内存扫描需明确 `--key-provider memory`。历史 `history` 和 `stats` 用于验证解密结果，而不是只检查密钥文件是否存在。

## 历史实测结果

针对测试群聊的 `history` 返回结果包含：

```json
{
  "chat_type": "group",
  "is_group": true,
  "count": 100,
  "meta": {
    "status": "ok",
    "shards_hit": 9,
    "shards_scanned": 9,
    "unknown_shards": [],
    "chat_latest_db": "message/message_8.db"
  }
}
```

当时 `stats` 进一步读取到该本机数据库中的 `13,105` 条消息，包含文本、图片、链接/文件、视频、语音和位置等类型。样本最新一条记录为 `2026-08-18 19:52`；原记录据此判断已到达当时的消息分片，而不是只读到旧缓存。该数量和时间不是本轮重新查询值。

因此，该历史样本可以证明当时：

- 该微信构建的密钥导出路径能够在样本机器上工作；
- 导出的密钥能够通过 SQLCipher 页级校验；
- 消息数据库的多个分片能够被实际解密和查询；
- `wx-cli` 的新版密钥提供器已经在集成后的命令行程序中生效。

## 只读内存扫描路径原理

以下解释的是 `memory` 及 `auto` 复用失败后的扫描路径，不适用于 Frida `account` 捕获；技术阈值已按当前源码复核，实机证据仍只限上方历史样本。

### 1. 先按微信文件版本选择路径

扫描开始时，程序先通过 Windows 进程 API 找到 `Weixin.exe`，读取进程映像路径，再读取 PE 文件中的 `VS_FIXEDFILEINFO` 版本资源。版本判断发生在扫描前，因此不会把新旧两套内存布局混在一起尝试。

当前阈值为 `4.1.10`：

- `4.1.9` 及更早版本继续使用旧的 raw-key 扫描器；
- `4.1.10` 及更新版本使用 `Config.Cipher` 扫描器。

### 2. 在微信进程中定位 `Config.Cipher`

新版微信不会像旧版本那样直接把可搜索的完整 `x'<key><salt>'` 字符串稳定地放在内存中。当前实现利用 WCDB 配置对象留下的字符串特征：

```text
com.Tencent.WCDB.Config.Cipher
```

程序使用 `VirtualQueryEx` 枚举微信进程中已提交且可读的内存区域，再通过 `ReadProcessMemory` 分块读取。找到上述字符串后，继续校验字符串对象的地址和长度，并沿对象引用找到 `Config.Cipher` 实例及其数据缓冲区。这样可以减少随机内存内容被误认为密钥材料的可能性。

### 3. 解码内存中的候选材料

`Config.Cipher` 对象中的数据不是最终数据库密钥，而是一个经过轻量变换的短数据块。当前版本中，该数据块经过固定 XOR 掩码还原后，包含类似下面的 WCDB 原始密钥材料：

```text
x'<64 个十六进制字符表示的 32 字节 key><32 个十六进制字符表示的 16 字节 salt>'
```

程序只接受符合结构的十六进制候选值，并从候选材料中提取 32 字节 raw key。这里的字符串特征和 XOR 解码规则属于当前微信构建的实现细节，不应假设在未来版本中永久不变。

### 4. 用数据库第一页验证候选 key

扫描内存得到的候选值不会直接写入密钥文件。程序会针对本机每个加密数据库读取第一页，并按微信 4.x 的 SQLCipher 参数进行 HMAC 校验：

1. 取数据库第一页前 16 字节作为 salt。
2. 将 salt 的每个字节与 `0x3a` 异或，得到 HMAC salt。
3. 使用候选 raw key 和该 HMAC salt 做 PBKDF2-HMAC-SHA512，迭代 2 次，得到 32 字节 MAC key。
4. 对第一页的加密内容、页码 `1` 计算 HMAC-SHA512。
5. 只有计算结果与数据库页尾保存的 HMAC 完全一致，才接受该候选 key。

微信 4.x 消息数据库使用 4096 字节页，第一页的 salt 是明文，页尾包含 IV 和 HMAC 保留区。实现还会优先检查当前 WAL 中对应的第一页，避免数据库主文件较旧时误判密钥。

因此，真正的验证标准不是“内存里找到了一段像 key 的十六进制字符串”，而是“候选 key 能通过数据库页级完整性校验，并能继续解密读取表内容”。

### 5. 完整匹配后才更新密钥文件

程序会将候选 key 与数据库 salt 建立对应关系，直到目标数据库全部完成校验才返回成功。新版路径没有旧方式回退：如果数据库没有全部验证通过，命令直接失败，已有 `all_keys.json` 不被新结果覆盖。

这套设计的作用是：

- 防止不同微信版本的内存结构被错误混用；
- 防止随机候选值污染密钥文件；
- 让 `history`、`stats` 等后续命令成为实际的解密验证，而不只是文件存在性检查。

### 6. 进程访问方式与边界

此处的 Windows 只读内存扫描路径只申请 `PROCESS_VM_READ` 和 `PROCESS_QUERY_INFORMATION` 权限，使用系统 API 读取进程内存，不注入 DLL、不写入微信进程内存，也不修改微信运行状态。显式 `account` 提供器不属于这一保证。但“只读”不等于对所有安全软件或微信版本完全不可感知，具体行为仍取决于操作系统、权限策略和微信后续更新。

## 版本选择策略

Windows 扫描器按 `Weixin.exe` 文件版本选择实现：

| 微信版本 | 密钥提供器 |
| --- | --- |
| `< 4.1.10` | 原有 legacy raw-key 扫描方式 |
| `>= 4.1.10` | `Config.Cipher` 只读扫描方式 |

新版扫描失败时不会尝试旧方式回退，而是直接退出，并保留已有密钥文件不变。这样可以避免新旧内存布局混用，也不会改变旧版本的原有路径。

新版扫描器只申请 `PROCESS_VM_READ` 和 `PROCESS_QUERY_INFORMATION` 权限，通过 `VirtualQueryEx` 枚举可读区域，再用 `ReadProcessMemory` 读取内容。当前实现不向微信进程注入代码，也不修改进程内存；这属于实现层面的只读保证，不代表能够对所有安全软件或微信版本承诺完全不可感知。

## 边界与后续验证

- 历史实测仅直接覆盖微信 `4.1.12.26` 与当时本地构建，不能据此保证当前代码或所有 `4.1.10+` 小版本具有相同兼容性。
- 微信必须在扫描时运行并完成登录；密钥扫描针对当前登录账号和当前机器。
- 图片、视频和语音的消息记录已经能够读取，但附件内容和语音转写需要单独执行附件解密或 ASR 流程。
- 不应将 `all_keys.json`、数据库文件或未脱敏聊天导出文件提交到 Git 仓库。
