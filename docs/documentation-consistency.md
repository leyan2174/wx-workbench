# 文档维护与一致性核查

2026-09-17 对照源码基线 `f6f3ba9` 修订。此次只改文档，不修改执行逻辑，也不把历史测试结果当作本轮重新运行的结果。

## 当前说明与历史记录

- README 负责安装、正式命令和使用限制；[架构说明](architecture.md)负责当前分层、运行路径和实现例外。
- `src` 下协议与模块契约负责参数、材料、发布与失败边界；相关使用文档同步这些契约。
- 日期化迁移报告及阶段验收报告保留当时状态，并标注阶段记录。旧目录、旧命令和旧测试数字在这些记录中是历史证据，不应批量替换成今天的状态。
- 独立文档仓库区分微信机制、项目实现与目标设计；源码发生变化后同时更新证据映射，不能仅复制架构结论。

## 本轮修正

1. 修复架构首页“worker 未统一”与后文现有 broker 链的冲突，明确初始化直接 Store 例外。
2. 更新初始化 Memory、Saved、Account 的边界，删除过时的 `force=false` 窄切片描述；README 首次初始化显式选择 memory。
3. 明确 Toolkit 源码层已删除，`wx toolkit` 仍是正式命令分组；补充分层表和 Mermaid 运行图。
4. 修正 MCP 配置锁生命周期和转录命令示例、语音适配器旧兼容转发描述。
5. 修复语音文档迁移造成的相对链接，更新旧架构图说明；为阶段报告添加历史标识。
6. 补充 AI 开发声明并保留第三方归属；发布治理同步“独立文档先 Private”的最新要求。

## 验证边界

检查覆盖根目录、docs、src 与 tests 下的 Markdown 本地文件链接和代码围栏；人工对照 main、business、application、初始化执行、worker 材料权限和 MCP 契约。Mermaid 图源在架构正文中，未以渲染成功代替运行验证。

本轮不改变 Rust，不重新宣称真实微信、媒体质量、云服务或全量行为测试通过。当前结构一致也不代表所有旧审查记录都已成为当前接口规范，更不证明微信服务器端密钥保存方式。
