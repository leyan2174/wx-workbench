# 安全报告与支持范围

本项目处理本机微信数据，仅面向用户自己拥有或明确获准的数据。当前支持 Windows x64 MSVC；具体行为及限制见 [README](README.md)。DPAPI 与账号路径隔离不构成抵御已控制当前 Windows 用户或管理员的安全边界。

请勿在公开 Issue 上传真实密钥、聊天记录、访问令牌、内存转储或包含私人路径的日志。若仓库显示 GitHub 的 “Report a vulnerability” 入口，可使用该私密入口；若未启用，请先用不含漏洞细节和秘密的消息联系维护者，请求私密接收渠道。本项目不声称已启用该平台功能，也不承诺固定响应时限。

报告应包含受影响版本、预期边界、脱敏后的复现步骤及影响。优先使用合成账号和临时目录，不测试未经授权的真实账号，不关闭其他用户进程或上传真实音频。

修复应保留账号绑定、宿主授权、输出目录保护、敏感材料脱敏与清零；不能以禁用校验或隐藏失败让测试通过。发布前检查候选及计划公开的历史，忽略文件规则不能代替隐私检查。
