diff --git a/tests/fixtures/native-attachment-security/attachment_refs.before-scalar-fix.txt b/src/toolkit/attachment_refs.rs
index 3d67998..b87e875 100644
--- a/tests/fixtures/native-attachment-security/attachment_refs.before-scalar-fix.txt
+++ b/src/toolkit/attachment_refs.rs
@@ -139,6 +139,20 @@ fn unique<'a, 'i>(mut nodes: impl Iterator<Item = Node<'a, 'i>>) -> Result<Optio
 fn text<'a, 'i>(node: Node<'a, 'i>, tag: &str) -> Result<&'a str> {
     Ok(child(node, tag)?.and_then(|n| n.text()).unwrap_or(""))
 }
+fn scalar_text(node: Option<Node<'_, '_>>) -> Result<String> {
+    let Some(node) = node else {
+        return Ok(String::new());
+    };
+    if node.children().any(|n| n.is_element()) {
+        return Err(error(ErrorKind::InvalidMetadata, "标量字段不能包含子元素"));
+    }
+    // Read all text/CDATA segments, including those separated by XML comments.
+    Ok(node
+        .children()
+        .filter(|n| n.is_text())
+        .filter_map(|n| n.text())
+        .collect())
+}
 fn descendant_text<'a, 'i>(node: Node<'a, 'i>, tag: &str) -> Result<&'a str> {
     Ok(
         unique(node.descendants().skip(1).filter(|n| named(*n, tag)))?
@@ -274,8 +288,10 @@ pub fn parse_file_message(input: &MessageInput<'_>) -> Result<AttachmentMetadata
         datatype: None,
         title,
         extension: xml::collapse(descendant_text(app, "fileext")?),
-        expected_size: number(descendant_text(app, "totallen")?)?,
-        expected_md5: hash(text(app, "md5")?)?,
+        expected_size: number(&scalar_text(unique(
+            app.descendants().skip(1).filter(|n| named(*n, "totallen")),
+        )?)?)?,
+        expected_md5: hash(&scalar_text(child(app, "md5")?)?)?,
         sender: String::new(),
         description: String::new(),
     })
@@ -325,8 +341,8 @@ pub fn parse_record_item(input: &MessageInput<'_>, item_index: i64) -> Result<At
         datatype: Some(datatype.into()),
         title: title(text(item, "datatitle")?)?,
         extension: xml::collapse(text(item, "datafmt")?),
-        expected_size: number(text(item, "datasize")?)?,
-        expected_md5: hash(text(item, "fullmd5")?)?,
+        expected_size: number(&scalar_text(child(item, "datasize")?)?)?,
+        expected_md5: hash(&scalar_text(child(item, "fullmd5")?)?)?,
         sender: xml::collapse(text(item, "sourcename")?),
         description: xml::collapse(text(item, "datadesc")?),
     })
