# wx-workbench 协作规则

本仓库只面向 Windows x64 MSVC。

- 修改 Rust 后运行 cargo check，不提交检查失败的 Rust 代码。
- 交付前运行 cargo check --target x86_64-pc-windows-msvc 和 cargo test。
- 不同 checkout/worktree 不得共用 CARGO_TARGET_DIR；同名 wx.exe 会相互覆盖，使真实进程测试失去可信度。runtime 夹具的 CARGO_BIN_EXE_wx 必须指向当前 checkout 的构建产物。
- 修改包版本时运行 cargo update --workspace。
- 保持账号隔离；不提交密钥、私人数据或解密缓存。
- 每次提交后推送到已配置的 origin 远端。
- 平台与发布目标仅限 Windows x64 MSVC；扩展支持范围须有明确需求和验证。

## 项目指挥与交付职责

- 用户指定的架构师任务兼任项目交付负责人，负责目标、架构边界、优先级、任务协调、独立验收、文档一致性和发布判断；编码任务负责实施与提供可复核证据。角色由当前任务分工确定，不能仅因读取本文件而另行接管指挥。
- 用户已授权负责人自主决定常规工程与发布准备事项。以用户可用的交付结果为目标，不反复询问命名、实现细节和一般任务安排。身份认证缺失、无法确认的权利等外部阻塞应清楚报告，不隐瞒、不伪造完成。
- 当前推进代码仓库发布准备和独立文档仓库交付；用户最新要求文档先发布为 Private，不自动改为 Public。交付记录与测试日志保存在仓库外；README 和接口文档只描述当前行为。
- 交付必须保证正确性、隐私与真实秘密保护、第三方分发依据、可复现构建及准确文档。未完成能力必须明确限定，不以删测试或虚假说明通过验收。
- 负责人下达任务时说明范围、依赖和验收；实施任务回报实际修改、检查结果、未完成项。收到最新目标时合并既有有效约束，避免重复整改和并行编辑同一范围。
- 架构图和文档区分已实现、目标设计、观察事实与待验证推断；公开文档对应确定的源码版本。保留第三方来源、许可证与版权归属。
- 公开发布前核查候选文件及计划公开的历史，不依赖 .gitignore 代替隐私审计。未知分发权利的第三方资产不进入已批准的公开候选；替代或缩减能力须在版本说明中写明。
- 源码公开、GitHub 二进制发布与 npm 发布分别管理。由负责人统一组织最终候选验收和公开操作；编码任务不得在验收过程中自行改变远端可见性或触发额外发包。提交与推送规则在已确定且核查过的分支/候选范围内执行。
- 不把测试总数等同于发布完成。最终报告应给出仓库链接、源码版本、文档版本、已通过检查、支持范围与剩余限制。
